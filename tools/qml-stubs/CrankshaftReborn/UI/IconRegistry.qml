import QtQuick 2.15

QtObject {
    function icon(name) { return null; }
}
import QtQuick 2.15

QtObject {
    // Minimal icon registry stub. Real registry provided at runtime.
    function icon(name) { return "" }
}
